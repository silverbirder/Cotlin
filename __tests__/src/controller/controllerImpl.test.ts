import IController from "../../../src/controller/iController";
import ControllerImpl from "../../../src/controller/controllerImpl";

describe('Class: ControllerImpl', () => {
    describe('Method: TODO', () => {
        test('Assert: TODO', () => {
            // Arrange
            const controller: IController = new ControllerImpl();

            // Act
            // Assert
        });
    })
});